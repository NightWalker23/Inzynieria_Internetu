{% extends 'base.html.twig' %}

{% block content %}
    <h1>Dodawanie wydawnictwa</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Dodaj" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('publisher_index') }}">Powrót do listy</a>
        </li>
    </ul>
{% endblock %}
